/// @description Init

event_inherited();

depth = 10;

hazard = true;
haz_color = 0;

accAttr = {
	Main : {
		Color : color,
		Angle : image_angle,
		Alpha : alpha,
		Group : group,
		Layer : editLayer,
		Locked : undefined,
		Condition : undefined,
		Speed : undefined,
		Trigger : undefined
	}
};
