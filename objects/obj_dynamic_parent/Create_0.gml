/// @description Init

event_inherited();

locked = false;

accAttr = {
	Main : {
		Color : color,
		Angle : image_angle,
		Alpha : alpha,
		Group : group,
		Layer : editLayer,
		Locked : locked,
		Condition : undefined,
		Speed : undefined,
		Trigger : undefined
	}
};
